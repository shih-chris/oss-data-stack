# dlt configuration file for the native DuckLake destination.
# Catalog/storage credentials are provided from environment variables in code.

[runtime]
# Enable progress bar for data loading
log_level = "INFO"
progress = "enlighten"
